# 即時競標系統 - 前端

這是一個使用 React + TypeScript + Vite + Tailwind CSS 構建的即時競標/限時搶購系統前端應用。

## 功能特色

- 🔐 **用戶認證系統**：支持 Member 和 Admin 兩種角色
- 📦 **商品大廳**：查看所有商品及其狀態（準備中/競標中/已結束）
- 🎯 **競標頁面**：單一商品詳細頁面，包含出價表單和即時排行榜
- 👨‍💼 **管理後台**：Admin 可以新增、編輯商品，切換商品狀態
- 📊 **即時更新**：模擬 WebSocket 連接，實時更新排行榜和商品狀態
- 🏆 **結果展示**：活動結束後顯示最終得標結果

## 技術棧

- **框架**: React 19.2
- **語言**: TypeScript
- **構建工具**: Vite 7.2
- **樣式**: Tailwind CSS 3.4
- **路由**: React Router DOM 7.1
- **狀態管理**: React Context API

## 快速開始

### 環境要求

- Node.js >= 18
- npm >= 9

### 安裝依賴

```bash
cd frontend
npm install
```

### 開發模式

```bash
npm run dev
```

應用將在 `http://localhost:5173` 啟動。

### 構建生產版本

```bash
npm run build
```

構建產物將輸出到 `dist/` 目錄。

### 預覽生產版本

```bash
npm run preview
```

### 代碼檢查

```bash
npm run lint
```

## 項目結構

```
frontend/
├── src/
│   ├── components/          # 共用組件
│   │   ├── Header.tsx       # 頂部導航欄
│   │   ├── BidForm.tsx      # 出價表單
│   │   ├── RankingTable.tsx # 排行榜表格
│   │   ├── ProductInfo.tsx  # 商品資訊卡片
│   │   ├── ResultSection.tsx # 結果展示區塊
│   │   └── ProtectedRoute.tsx # 路由保護組件
│   ├── pages/              # 頁面組件
│   │   ├── login.tsx       # 登入頁
│   │   ├── ProductLobbyPage.tsx # 商品大廳
│   │   ├── AuctionDetailPage.tsx # 競標詳情頁
│   │   └── AdminProductPage.tsx # 管理後台
│   ├── contexts/           # Context 提供者
│   │   └── AuthContext.tsx # 認證上下文
│   ├── services/           # 服務層
│   │   ├── mockProductService.ts # 商品服務（Mock）
│   │   └── mockWebSocket.ts # WebSocket 服務（Mock）
│   ├── types/              # TypeScript 類型定義
│   │   └── product.ts      # 商品相關類型
│   ├── App.tsx             # 主應用組件
│   └── main.tsx            # 應用入口
├── public/                  # 靜態資源
├── package.json
├── vite.config.ts          # Vite 配置
├── tailwind.config.js      # Tailwind 配置
└── tsconfig.json           # TypeScript 配置
```

## 路由結構

- `/login` - 登入頁面
- `/products` - 商品大廳（需要登入）
- `/auction/:productId` - 商品競標詳情頁（需要登入）
- `/admin/products` - 管理後台（需要 Admin 角色）

## Docker 部署

### 開發環境

```bash
docker build -f Dockerfile.dev -t rtb-frontend-dev .
docker run -p 5173:5173 rtb-frontend-dev
```

### 生產環境

```bash
docker build -t rtb-frontend .
docker run -p 80:80 rtb-frontend
```

## 環境變數

目前使用 Mock 數據，後續可通過環境變數配置 API 端點：

```env
VITE_API_BASE_URL=http://localhost:8000
VITE_WS_URL=ws://localhost:8000/ws
```

## 後端 API 整合

目前所有數據使用 Mock 服務。要整合真實後端 API，請參考 `API_SPEC.md` 文件。

主要需要替換的服務：
- `src/contexts/AuthContext.tsx` - 登入 API
- `src/services/mockProductService.ts` - 商品 CRUD API
- `src/services/mockWebSocket.ts` - WebSocket 連接

## 開發注意事項

1. **類型安全**：所有組件和函數都使用 TypeScript 類型定義
2. **狀態管理**：使用 React Context 管理全局狀態（用戶認證）
3. **路由保護**：使用 `ProtectedRoute` 組件保護需要登入的頁面
4. **響應式設計**：使用 Tailwind CSS 實現響應式布局
5. **Mock 數據**：開發階段使用 Mock 服務，便於前端獨立開發

## 常見問題

### Q: 如何修改 API 端點？

A: 在 `src/services/` 目錄下的服務文件中，將 Mock 調用替換為真實 API 調用。

### Q: 如何添加新的商品狀態？

A: 在 `src/types/product.ts` 中修改 `ProductStatus` 類型，並更新相關組件。

### Q: 如何自定義樣式？

A: 修改 `tailwind.config.js` 或直接在組件中使用 Tailwind 類名。

## 授權

MIT License
